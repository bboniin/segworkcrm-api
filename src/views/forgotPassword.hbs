<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        .registration {
            max-width: 600px;
            width: 100%;
            margin: 0 auto;
            margin-bottom: 20px;
        }

        .msg-principal {
            color: #000;
            text-align: center;
            font-size: 23px;
            padding: 17px;
        }

        .text-principal {
            text-align: center;
            color: #363636;
            font-weight: 600;
            margin: 22px;
        }

        .text-destaque {
            margin-bottom: 16px;
        }

        .text-destaque span {
            font-weight: bold;
        }

        .text-auxiliar {
            font-weight: 400;
            max-width: 350px;
            margin: 18px auto;
        }

        .footer {
            max-width: 600px;
            width: 100%;
            margin: 0 auto;
            padding: 11px 22px;
            border-radius: 4px;
            align-items: center;
        }

        .footer div {
            text-align: center;
        }

        .footer a {
            margin: 0 15px;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div class="registration">
        <div class="msg-principal">
            <span>Solicitação de Recuperação de Senha</span>
        </div>

        <div class="text-principal">
            <p class="text-destaque"> Olá, <span> {{name}}</span> </p>

            <p class="text-destaque">O link para recuperação da senha é</p>

            <a class="text-destaque" href="https://app.segworkcrm.com.br/resetpassword/{{code}}"
                style="font-size:20px;">https://app.segworkcrm.com.br/resetpassword/{{code}}</a>

            <p class="text-auxiliar">Atenciosamente</p>

            <p class="text-auxiliar">Equipe SegWorkCRM</p>
        </div>
    </div>
</body>